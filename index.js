function dirttool($path){
  if(!is_dir($path)) {//checks to see if it is a directory
exit("<br><strong>$path is NOT a directory</strong>");
  }// end of if
$this->path=$path;
 $from ="";
  $to ="";
  //reads the directory
  $slash =$slash();
  $verz =opendir($path);
  while ($file = readdir($verz)) {
  if ()
  }
}